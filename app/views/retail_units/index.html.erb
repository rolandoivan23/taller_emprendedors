<h1>Listing retail_units</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Descripcion</th>
    <th>Tipo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @retail_units.each do |retail_unit| %>
  <tr>
    <td><%= retail_unit.nombre %></td>
    <td><%= retail_unit.descripcion %></td>
    <td><%= retail_unit.tipo %></td>
    <td><%= link_to 'Show', retail_unit %></td>
    <td><%= link_to 'Edit', edit_retail_unit_path(retail_unit) %></td>
    <td><%= link_to 'Destroy', retail_unit, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Retail unit', new_retail_unit_path %>
